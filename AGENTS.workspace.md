# Codex 工作区总管理目录

> 此目录 `E:\AI\AI Coding\WorkSpace for Codex` 统一管理不同方向的子项目。
> 全局通用规范请参见 `C:\Users\30782\.codex\AGENTS.md`。
> 各子项目在各自目录内维护自己的 `AGENTS.md`，仅包含该项目特有的规则。

## 📁 工作区层级约定

- **子项目隔离**：所有子项目在对应子文件夹内执行与处理，不在根目录直接开展工作
- **过程文件归属**：临时脚本、中间结果、提取文本、调试产物统一放入对应子项目目录或其 `_过程文件/` 目录
- **根目录仅保留**：工作区级说明文件、必要的全局配置、真正需要跨项目共享的公共资源

## 🔗 子项目自动发现机制

> **子项目自动发现机制**：工作区根目录下所有包含 `AGENTS.md` 的子目录即为独立子项目。
> 新增子项目时，按约定创建文件夹与 `AGENTS.md` 即可自动被识别，无需维护此列表。

---

## 📦 Git 多仓库管理规范

> **架构决策**：采用 **多仓库模式**。工作区根目录不初始化 Git，每个顶层子项目独立维护一个 Git 仓库。
> 子项目内的子目录（如 `三角洲小工具/三角洲枪械消耗/`）不单独建仓，归属父项目仓库管理。

### 1. 仓库初始化
- 新建子项目时，在子项目根目录执行 `git init`
- 首次提交包含：`AGENTS.md`、项目基础结构、`.gitignore`
- 不在工作区根目录执行 `git init`，不使用 submodule/subtree

### 2. 分支策略
| 分支 | 用途 | 保护 |
|------|------|------|
| `main` | 生产就绪代码，仅接受合并提交 | 受保护，禁止直接推送 |
| `dev` | 日常开发集成分支 | 可直接推送 |
| `feature/*` | 功能分支，从 `dev` 切出，合并回 `dev` | 完成后删除 |
| `hotfix/*` | 紧急修复，从 `main` 切出，合并回 `main` 并回合 `dev` | 完成后删除 |

> 小项目可简化为仅 `main` + `feature/*`，视复杂度决定。

### 3. 提交规范 (Conventional Commits 简化版)
```
<type>(<scope>): <subject>

<body> (可选)
<footer> (可选)
```

**type 必选值**：
- `feat` — 新功能
- `fix` — Bug 修复
- `refactor` — 重构（非功能、非修复）
- `docs` — 文档变更
- `style` — 格式调整（不影响逻辑）
- `test` — 测试相关
- `chore` — 构建/工具/依赖更新
- `perf` — 性能优化

**示例**：
```
feat(parser): 支持 9×19mm 特殊弹 CT 匹配
fix(calc): 修复 A+ 级改装费浮动系数取值错误
docs: 更新三角洲枪械消耗运行方式说明
```

### 4. 版本号与标签
- 遵循全局 `x.y.z` 语义化版本（见全局 AGENTS.md）
- 打标签格式：`v1.2.3`（前缀 `v`）
- 仅在 `main` 分支打版本标签
- 标签信息包含：版本号、变更摘要、日期

### 5. Git 存档规则（对应全局版本规则）
| 版本变更 | 操作 |
|----------|------|
| **大版本 (x)** | 必须打标签 `vX.0.0`，生成 CHANGELOG，归档发布 |
| **稳定版 (y)** | 必须打标签 `vX.Y.0`，更新 CHANGELOG |
| **小版本 (z)** | **建议打标签 `vX.Y.Z`**，至少保留关键修复的提交记录 |

> 使用 `git tag -a v1.2.3 -m "v1.2.3: 修复耗弹量公式边界值问题"` 创建附注标签

### 6. .gitignore 通用原则
每个项目必须包含 `.gitignore`，至少覆盖：
```
# 依赖与构建产物
node_modules/
dist/
build/
*.egg-info/
__pycache__/
*.pyc

# 缓存与临时数据
*.json (缓存文件如 ammo4.json/ammo5.json/guns.json)
*.tmp
*.log
last_update.txt

# 过程文件
_过程文件/
*.tmp.py

# IDE/OS
.vs/
.vscode/
.DS_Store
Thumbs.db

# 本地配置（含敏感信息/机器相关路径）
local.properties
*.local.env
```

### 7. 提交前自检清单
- [ ] 代码通过 lint / 类型检查
- [ ] 相关测试通过
- [ ] 提交信息符合规范
- [ ] 无调试代码、硬编码路径、print/log 残留
- [ ] 敏感信息未提交（API Key、密钥、本机路径）

### 8. 协作与远程
- 远程仓库托管于 GitHub/GitLab/Gitee，按项目决定公开/私有
- `main` 分支开启保护：要求 PR + Review + CI 通过才能合并
- 使用 GitHub Actions / GitLab CI 做基础 CI（lint + test + build）

### 9. 子项目内的子目录管理
- 如 `三角洲小工具/三角洲枪械消耗/`：**不单独建仓**，归属父项目 `三角洲小工具` 仓库
- 子目录的版本号、CHANGELOG 可在父项目中按模块维护，或共用父项目版本号
- 若子目录未来独立部署/发布，再考虑拆分为独立仓库

---

> 新增子项目前，请先确认项目名，在子项目根目录执行 `git init`，并按上述规范创建对应文件夹与 `AGENTS.md`。